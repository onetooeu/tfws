# Autonomous operations safety

The Governor coordinates specialist agents. A deterministic policy engine and independent
Sentinel enforce non-bypassable boundaries. No agent can access offline root/recovery keys,
rewrite audit history, raise its own budget, disable the kill switch, or approve root recovery.
