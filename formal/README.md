# Formal models

These TLA+ models encode initial safety invariants for key lifecycle and agent authority. They are
review artifacts, not proof certificates. Production release requires TLC runs with bounded models,
reviewed invariants, counterexample retention and linkage to implementation tests.
